//const math = require('./math'); //no need .js
//module.exports will return into require() and assign to math
// console.log(math.add(3,3));

const colors = require('colors')

const cats = require('./shelter');

console.log("required directory",cats);